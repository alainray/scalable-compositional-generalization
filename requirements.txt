# Quick pip/venv setup for this project.
# Usage:
#   python -m venv .venv
#   source .venv/bin/activate
#   python -m pip install --upgrade pip
#   python -m pip install -r requirements.txt
#
# The PyTorch wheels below install the default pip build for your platform.
# If you need a specific CUDA build, follow https://pytorch.org/get-started/locally/
# and then install the remaining packages from this file.

-e .

dataframe-image==0.2.4
dictdiffer==0.9.0
h5py==3.12.1
matplotlib==3.10.0
numpy==1.26.4
omegaconf==2.3.0
opencv-python==4.8.1.78
ortools==9.10.4067
pandas==2.2.3
Pillow==11.1.0
PyYAML==6.0.2
scikit-learn==1.5.2
seaborn==0.13.2
tensorboard==2.17.0
torch==2.3.0
torchvision==0.18.0
tqdm==4.66.5
wandb==0.19.3
