import os,random,numpy as np,torch
def fix_random(seed):'Fix random seed for a bunch of libraries.\n    Args:\n        seed (int): random seed\n    ';torch.manual_seed(seed);np.random.seed(seed);random.seed(seed);os.environ['PYTHONHASHSEED']=str(seed);os.environ.setdefault('CUBLAS_WORKSPACE_CONFIG',':16:8');torch.cuda.manual_seed(seed);torch.cuda.manual_seed_all(seed);torch.backends.cudnn.deterministic=True;torch.backends.cudnn.benchmark=False;torch.use_deterministic_algorithms(True, warn_only=True)
